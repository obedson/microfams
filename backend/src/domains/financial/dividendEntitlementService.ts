import { supabase } from '../../utils/supabase.js';
export class DividendValidationError extends Error {}
export class DividendEntitlementService{async calculate(q:any){if(!q.sourcePeriod||!q.distributionKey||!q.currency||!Array.isArray(q.eligibility)||!q.eligibility.length||!q.idempotencyKey)throw new DividendValidationError('Dividend calculation request is invalid.');const{data,error}=await supabase.rpc('calculate_dividend_entitlement_snapshot',{p_organization:q.organizationId,p_actor:q.actorId,p_source_period:q.sourcePeriod,p_distribution_key:q.distributionKey,p_currency:q.currency.toUpperCase(),p_distributable_minor:q.distributableMinor,p_retained_reserve_minor:q.retainedReserveMinor,p_record_date:q.recordDate,p_payment_date:q.paymentDate,p_withholding_rule:q.withholdingRule||{},p_eligibility:q.eligibility,p_idempotency_key:q.idempotencyKey,p_calculated_at:q.calculatedAt||new Date().toISOString()});if(error)throw error;return{distributionId:data};}}
export const dividendEntitlementService=new DividendEntitlementService();
