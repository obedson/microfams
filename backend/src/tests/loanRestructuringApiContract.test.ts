import {LoanRestructuringController} from '../controllers/loanRestructuringController.js';import {LoanRestructuringService} from '../domains/financial/loanRestructuringService.js';
const response=()=>{const res:any={};res.status=jest.fn().mockReturnValue(res);res.json=jest.fn().mockReturnValue(res);return res;};
describe('loan restructuring API',()=>{
it('takes tenant and actor only from authentication context',async()=>{const service={propose:jest.fn().mockResolvedValue({state:'proposed'}),decide:jest.fn()} as unknown as jest.Mocked<LoanRestructuringService>;const res=response();await new LoanRestructuringController(service).propose({tenant:{id:'00000000-0000-4000-8000-000000000911'},user:{id:'00000000-0000-4000-8000-000000000912'},params:{applicationId:'00000000-0000-4000-8000-000000000913',contractId:'00000000-0000-4000-8000-000000000914'},body:{organizationId:'untrusted',actorId:'untrusted',effectiveDate:'2026-08-13',firstDueDate:'2026-09-13',installmentCount:4,reasonCode:'BORROWER_HARDSHIP',reason:'Verified hardship supports revised future due dates.',evidenceReferences:['case:9'],correlationId:'00000000-0000-4000-8000-000000000915',idempotencyKey:'restructure-api-1'}} as any,res);expect(service.propose).toHaveBeenCalledWith(expect.objectContaining({organizationId:'00000000-0000-4000-8000-000000000911',actorId:'00000000-0000-4000-8000-000000000912'}));expect(res.status).toHaveBeenCalledWith(201);});
it('rejects incomplete decisions before storage',async()=>{const service={propose:jest.fn(),decide:jest.fn()} as unknown as jest.Mocked<LoanRestructuringService>;const res=response();await new LoanRestructuringController(service).decide({tenant:{id:'x'},user:{id:'y'},params:{},body:{}} as any,res);expect(res.status).toHaveBeenCalledWith(400);expect(service.decide).not.toHaveBeenCalled();});
});
