import'@testing-library/jest-dom';import React from'react';import{fireEvent,render,screen,waitFor}from'@testing-library/react';import SavingsAccruals from'./SavingsAccruals';import{savingsAccrualAPI}from'../services/savingsAccrualAPI';jest.mock('../services/savingsAccrualAPI',()=>({savingsAccrualAPI:{list:jest.fn().mockResolvedValue([]),calculate:jest.fn().mockResolvedValue({})}}));test('calculates a savings accrual batch',async()=>{render(<SavingsAccruals/>);fireEvent.change(screen.getByLabelText('Product version ID'),{target:{value:'version-1'}});fireEvent.change(screen.getByLabelText('Period start'),{target:{value:'2028-01-01'}});fireEvent.change(screen.getByLabelText('Period end'),{target:{value:'2028-01-31'}});fireEvent.click(screen.getByRole('button',{name:'Calculate accrual'}));await waitFor(()=>expect(savingsAccrualAPI.calculate).toHaveBeenCalled());});
