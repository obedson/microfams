import { InvestmentAllocationPlanController } from '../controllers/investmentAllocationPlanController.js';
describe('Investment allocation plan API contract',()=>{it('exposes planning, approval, refund-recognition, and execution handlers',()=>{const c=new InvestmentAllocationPlanController({} as any);expect(c.create).toBeInstanceOf(Function);expect(c.approve).toBeInstanceOf(Function);expect(c.recognizeRefunds).toBeInstanceOf(Function);expect(c.execute).toBeInstanceOf(Function);});});
