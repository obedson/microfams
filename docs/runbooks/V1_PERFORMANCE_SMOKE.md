# V1 performance smoke evidence

The backend CI includes a deterministic performance smoke test covering 500 provider-neutral intelligence service operations with a one-second CI budget. It detects gross synchronous regressions without external network or database variability. This is not production load, concurrency, soak, capacity, or provider certification; those remain required before V1 GO.
