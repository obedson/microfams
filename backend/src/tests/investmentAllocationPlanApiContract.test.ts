import { InvestmentAllocationPlanController } from '../controllers/investmentAllocationPlanController.js';
describe('Investment allocation plan API contract',()=>{it('exposes planning, approval, and refund-recognition handlers',()=>{const c=new InvestmentAllocationPlanController({} as any);expect(c.create).toBeInstanceOf(Function);expect(c.approve).toBeInstanceOf(Function);expect(c.recognizeRefunds).toBeInstanceOf(Function);});});
