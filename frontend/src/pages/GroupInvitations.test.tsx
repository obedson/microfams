import'@testing-library/jest-dom';import React from'react';import{fireEvent,render,screen,waitFor}from'@testing-library/react';import GroupInvitations from'./GroupInvitations';import{groupInvitationAPI}from'../services/groupInvitationAPI';jest.mock('../services/groupInvitationAPI',()=>({groupInvitationAPI:{list:jest.fn(),create:jest.fn(),accept:jest.fn(),revoke:jest.fn()}}));test('creates a group invitation',async()=>{(groupInvitationAPI.list as jest.Mock).mockResolvedValue({data:[]});(groupInvitationAPI.create as jest.Mock).mockResolvedValue({});render(<GroupInvitations/>);for(const[l,v]of[['Group ID','group-1'],['Intended user ID','user-1'],['Expires at','2028-12-01T12:00'],['Idempotency key','invite-key-1']])fireEvent.change(screen.getByLabelText(l),{target:{value:v}});fireEvent.click(screen.getByRole('button',{name:'Create invitation'}));await waitFor(()=>expect(groupInvitationAPI.create).toHaveBeenCalledWith('group-1',expect.objectContaining({intendedUserId:'user-1'}),'invite-key-1'));expect(await screen.findByRole('status')).toHaveTextContent('created');});