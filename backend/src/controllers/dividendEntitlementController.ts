import{Response}from'express';import{TenantRequest}from'../middleware/tenant.js';import{DividendValidationError,dividendEntitlementService}from'../domains/financial/dividendEntitlementService.js';const run=(action:'calculate'|'review'|'approve'|'recognizePayable')=>(req:TenantRequest,res:Response)=>dividendEntitlementService[action]({...req.body,organizationId:req.tenant!.id,actorId:req.user!.id}).then(result=>res.status(action==='calculate'?201:200).json(result)).catch(e=>e instanceof DividendValidationError?res.status(400).json({error:e.message}):res.status(409).json({error:`DIVIDEND_${action.toUpperCase()}_UNAVAILABLE`}));export const dividendEntitlementController={calculate:run('calculate'),review:run('review'),approve:run('approve'),recognizePayable:run('recognizePayable')};
