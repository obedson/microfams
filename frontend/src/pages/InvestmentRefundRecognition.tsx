import React,{useState}from'react';import{investmentAllocationPlanAPI}from'../services/investmentAllocationPlanAPI';const uuid=()=>typeof crypto!=='undefined'&&typeof crypto.randomUUID==='function'?crypto.randomUUID():'00000000-0000-4000-8000-000000000001';export default function InvestmentRefundRecognition(){const[plan,setPlan]=useState(''),[key,setKey]=useState(''),[notice,setNotice]=useState(''),[error,setError]=useState('');const submit=async(e:any)=>{e.preventDefault();setError('');try{const r=await investmentAllocationPlanAPI.recognize(plan,{correlationId:uuid(),idempotencyKey:key});setNotice('Refund obligations recognized'+(r?.run?.obligation_count!==undefined?' ('+r.run.obligation_count+')':'')+'.')}catch{setError('Refund-obligation recognition was rejected. Check plan state, tenant permissions, feature flags, and settlement evidence.')}};return <main><h1>Investment refund obligations</h1><p>Recognize settlement-linked refund liabilities before any provider submission or unit execution.</p>{error&&<p role="alert">{error}</p>}{notice&&<p role="status">{notice}</p>}<form onSubmit={submit}><input aria-label="Allocation plan ID" value={plan} onChange={e=>setPlan(e.target.value)} required/><input aria-label="Idempotency key" value={key} onChange={e=>setKey(e.target.value)} required/><button>Recognize refund obligations</button></form></main>}
