import React,{FormEvent,useState} from 'react'; import {accountingAPI,IncomeStatement as Report} from '../services/accountingAPI';
const money=(minor:string,currency:string)=>new Intl.NumberFormat(undefined,{style:'currency',currency}).format(Number(minor)/100);
export default function IncomeStatement(){const [currency,setCurrency]=useState('NGN'),[from,setFrom]=useState(''),[to,setTo]=useState(''),[cutoff,setCutoff]=useState(''),[report,setReport]=useState<Report|null>(null),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{setReport(await accountingAPI.incomeStatement({currency,from,to,cutoff:new Date(cutoff).toISOString()}));}catch{setError('The income statement could not be generated for this period and cutoff.');}finally{setBusy(false);}};return <main className="mx-auto max-w-5xl px-4 py-10"><h1 className="text-3xl font-black">Income statement</h1><p className="mt-2 text-gray-600">Journal-derived results reproduced at an explicit cutoff.</p><form onSubmit={submit} className="mt-6 grid gap-4 rounded-lg border bg-white p-5 md:grid-cols-4"><label>Currency<input aria-label="Currency" value={currency} onChange={e=>setCurrency(e.target.value.toUpperCase())} className="mt-1 w-full rounded border p-2" /></label><label>From<input aria-label="From" type="date" required value={from} onChange={e=>setFrom(e.target.value)} className="mt-1 w-full rounded border p-2" /></label><label>To<input aria-label="To" type="date" required value={to} onChange={e=>setTo(e.target.value)} className="mt-1 w-full rounded border p-2" /></label><label>Cutoff<input aria-label="Cutoff" type="datetime-local" required value={cutoff} onChange={e=>setCutoff(e.target.value)} className="mt-1 w-full rounded border p-2" /></label><button disabled={busy} className="rounded bg-primary-700 px-4 py-3 font-bold text-white md:col-span-4">{busy?'Generating...':'Generate statement'}</button></form>{error&&<p role="alert" className="mt-4 rounded bg-red-50 p-4 text-red-800">{error}</p>}{report&&<section className="mt-8 rounded-lg border bg-white p-6"><h2 className="text-xl font-bold">{report.period.name}</h2><p className="text-sm text-gray-500">Cutoff {new Date(report.cutoff).toLocaleString()}</p><dl className="mt-5 grid gap-4 md:grid-cols-3"><div><dt>Total revenue</dt><dd className="text-2xl font-black">{money(report.totalRevenueMinor,report.currency)}</dd></div><div><dt>Total expenses</dt><dd className="text-2xl font-black">{money(report.totalExpenseMinor,report.currency)}</dd></div><div><dt>Net income</dt><dd className="text-2xl font-black">{money(report.netIncomeMinor,report.currency)}</dd></div></dl><table className="mt-6 min-w-full text-left text-sm"><thead><tr><th>Account</th><th>Class</th><th>Amount</th></tr></thead><tbody>{report.revenue.map(line=><tr key={line.accountId}><td>{line.code} · {line.name}</td><td className="capitalize">{line.accountClass}</td><td>{money(line.amountMinor,report.currency)}</td></tr>)}</tbody></table></section>}</main>}
