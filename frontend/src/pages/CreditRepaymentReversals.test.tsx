import '@testing-library/jest-dom';import React from'react';import{fireEvent,render,screen,waitFor}from'@testing-library/react';import CreditRepaymentReversals from'./CreditRepaymentReversals';import{creditRepaymentReversalAPI}from'../services/creditRepaymentReversalAPI';jest.mock('../services/creditRepaymentReversalAPI',()=>({creditRepaymentReversalAPI:{propose:jest.fn(),decide:jest.fn()}}));test('records proposal then decision',async()=>{(creditRepaymentReversalAPI.propose as jest.Mock).mockResolvedValue({reversalId:'r1'});(creditRepaymentReversalAPI.decide as jest.Mock).mockResolvedValue({state:'approved'});render(<CreditRepaymentReversals/>);for(const [l,v] of [['Application ID','a'],['Contract ID','c'],['Repayment ID','r'],['Idempotency key','key-12345']])fireEvent.change(screen.getByLabelText(l),{target:{value:v}});fireEvent.click(screen.getByRole('button',{name:'Record reversal proposal'}));expect(await screen.findByText('Reversal proposal recorded.')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:'Record decision'}));await waitFor(()=>expect(creditRepaymentReversalAPI.decide).toHaveBeenCalled());});