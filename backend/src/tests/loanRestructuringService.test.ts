import {LoanRestructuringService,LoanRestructuringValidationError} from '../domains/financial/loanRestructuringService.js';
const ids={organizationId:'00000000-0000-4000-8000-000000000901',actorId:'00000000-0000-4000-8000-000000000902',applicationId:'00000000-0000-4000-8000-000000000903',contractId:'00000000-0000-4000-8000-000000000904',correlationId:'00000000-0000-4000-8000-000000000905'};
describe('loan restructuring service',()=>{
it('passes governed proposal evidence to storage',async()=>{const gateway={propose:jest.fn().mockResolvedValue({state:'proposed'}),decide:jest.fn()};const command={...ids,effectiveDate:'2026-08-13',firstDueDate:'2026-09-13',installmentCount:4,reasonCode:'BORROWER_HARDSHIP',reason:'Verified hardship supports revised future due dates.',evidenceReferences:['case:9'],idempotencyKey:'restructure-proposal-1'};await expect(new LoanRestructuringService(gateway).propose(command)).resolves.toEqual({state:'proposed'});expect(gateway.propose).toHaveBeenCalledWith(command);});
it('passes independent decision evidence to storage',async()=>{const gateway={propose:jest.fn(),decide:jest.fn().mockResolvedValue({state:'approved'})};const command={organizationId:ids.organizationId,actorId:ids.actorId,restructureId:'00000000-0000-4000-8000-000000000906',decision:'approve' as const,reviewReason:'Independent review confirms the revised schedule is suitable.',correlationId:ids.correlationId,idempotencyKey:'restructure-decision-1'};await expect(new LoanRestructuringService(gateway).decide(command)).resolves.toEqual({state:'approved'});});
it('rejects an invalid date sequence',()=>{const gateway={propose:jest.fn(),decide:jest.fn()};expect(()=>new LoanRestructuringService(gateway).propose({...ids,effectiveDate:'2026-09-13',firstDueDate:'2026-08-13',installmentCount:4,reasonCode:'BORROWER_HARDSHIP',reason:'Verified hardship supports revised future due dates.',evidenceReferences:['case:9'],idempotencyKey:'restructure-proposal-1'})).toThrow(LoanRestructuringValidationError);});
});
