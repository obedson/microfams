import'@testing-library/jest-dom';import React from'react';import{fireEvent,render,screen,waitFor}from'@testing-library/react';import BookingDispute from'./BookingDispute';import{bookingDisputeAPI}from'../services/bookingDisputeAPI';jest.mock('../services/bookingDisputeAPI',()=>({bookingDisputeAPI:{open:jest.fn(),timeline:jest.fn(),addEvidence:jest.fn()}}));test('opens a booking dispute',async()=>{(bookingDisputeAPI.open as jest.Mock).mockResolvedValue({data:{id:'dispute-1'}});render(<BookingDispute/>);for(const[l,v]of[['Booking ID','booking-1'],['Narrative','The booked property was unavailable when access was required.'],['Contested amount minor','5000'],['Idempotency key','dispute-key-1']])fireEvent.change(screen.getByLabelText(l),{target:{value:v}});fireEvent.click(screen.getByRole('button',{name:'Open dispute'}));await waitFor(()=>expect(bookingDisputeAPI.open).toHaveBeenCalledWith('booking-1',expect.objectContaining({contested_amount_minor:5000}),'dispute-key-1'));expect(await screen.findByRole('status')).toHaveTextContent('frozen');});