import{apiClient}from'../api/client';const headers=(key:string)=>({'Idempotency-Key':key});export const bookingDisputeAPI={open:async(bookingId:string,body:Record<string,unknown>,key:string)=>(await apiClient.post('/bookings/'+bookingId+'/disputes',body,{headers:headers(key)})).data,timeline:async(bookingId:string)=>(await apiClient.get('/bookings/'+bookingId+'/disputes')).data,addEvidence:async(disputeId:string,body:Record<string,unknown>,key:string)=>(await apiClient.post('/bookings/disputes/'+disputeId+'/evidence',body,{headers:headers(key)})).data};
