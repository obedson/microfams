import{dividendEntitlementService}from'../domains/financial/dividendEntitlementService.js';import{supabase}from'../utils/supabase.js';jest.mock('../utils/supabase.js',()=>({supabase:{rpc:jest.fn()}}));test('recognizes approved dividend payable through balanced posting contract',async()=>{(supabase.rpc as jest.Mock).mockResolvedValue({data:'journal-1',error:null});await expect(dividendEntitlementService.recognizePayable({organizationId:'o',actorId:'a',distributionId:'d',retainedSurplusAccount:'r',dividendsPayableAccount:'p',effectiveDate:'2028-12-01',idempotencyKey:'payable-2028',correlation:'00000000-0000-4000-8000-000000000001'})).resolves.toEqual({journalEntryId:'journal-1',state:'payable'});expect(supabase.rpc).toHaveBeenCalledWith('recognize_dividend_payable',expect.objectContaining({p_distribution:'d',p_retained_surplus_account:'r',p_dividends_payable_account:'p'}));});
