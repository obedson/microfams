import{dividendEntitlementService}from'../domains/financial/dividendEntitlementService.js';import{supabase}from'../utils/supabase.js';jest.mock('../utils/supabase.js',()=>({supabase:{rpc:jest.fn()}}));test('pays an approved dividend through internal-wallet journal contract',async()=>{(supabase.rpc as jest.Mock).mockResolvedValue({data:{distributionId:'d1',state:'paid',journalEntryIds:['j1']},error:null});await expect(dividendEntitlementService.pay({organizationId:'o',actorId:'a',distributionId:'d1',effectiveDate:'2028-12-15',correlation:'00000000-0000-4000-8000-000000000001'})).resolves.toEqual(expect.objectContaining({state:'paid'}));expect(supabase.rpc).toHaveBeenCalledWith('pay_dividend_distribution',expect.objectContaining({p_distribution:'d1'}));});
