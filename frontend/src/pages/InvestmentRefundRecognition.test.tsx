import'@testing-library/jest-dom';import React from'react';import{fireEvent,render,screen,waitFor}from'@testing-library/react';import InvestmentRefundRecognition from'./InvestmentRefundRecognition';import{investmentAllocationPlanAPI}from'../services/investmentAllocationPlanAPI';jest.mock('../services/investmentAllocationPlanAPI',()=>({investmentAllocationPlanAPI:{recognize:jest.fn()}}));test('recognizes settlement-linked refund obligations',async()=>{(investmentAllocationPlanAPI.recognize as jest.Mock).mockResolvedValue({run:{obligation_count:2}});render(<InvestmentRefundRecognition/>);fireEvent.change(screen.getByLabelText('Allocation plan ID'),{target:{value:'plan-1'}});fireEvent.change(screen.getByLabelText('Idempotency key'),{target:{value:'refund-recognition-1'}});fireEvent.click(screen.getByRole('button',{name:'Recognize refund obligations'}));await waitFor(()=>expect(investmentAllocationPlanAPI.recognize).toHaveBeenCalledWith('plan-1',expect.objectContaining({idempotencyKey:'refund-recognition-1'})));expect(await screen.findByRole('status')).toHaveTextContent('2');});