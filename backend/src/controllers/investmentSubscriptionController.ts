import { Response } from 'express'; import Joi from 'joi'; import { CreateInvestmentSubscriptionCommand,InvestmentSubscriptionService,InvestmentSubscriptionValidationError,investmentSubscriptionService } from '../domains/financial/investmentSubscriptionService.js'; import { TenantRequest } from '../middleware/tenant.js';
const schema=Joi.object({requestedAmountMinor:Joi.number().integer().min(1).required(),investorCountry:Joi.string().pattern(/^[A-Z]{2}$/).required(),investorType:Joi.string().valid('individual','group','organization').required(),acceptedRiskDisclosureVersion:Joi.string().trim().min(1).max(80).required(),acceptedRiskDisclosureHash:Joi.string().pattern(/^[a-f0-9]{64}$/).required(),correlationId:Joi.string().uuid().required(),idempotencyKey:Joi.string().min(8).max(160).required()});
export class InvestmentSubscriptionController { constructor(private readonly service:InvestmentSubscriptionService=investmentSubscriptionService){} create=async(req:TenantRequest,res:Response)=>{const {error,value}=schema.validate(req.body,{abortEarly:false,stripUnknown:true});if(error)return res.status(400).json({success:false,error:'INVALID_INVESTMENT_SUBSCRIPTION_COMMAND',details:error.details.map(d=>d.message)});try{return res.status(201).json(await this.service.create({...value,organizationId:req.tenant!.id,actorId:req.user!.id,productId:req.params.productId} as CreateInvestmentSubscriptionCommand));}catch(e){if(e instanceof InvestmentSubscriptionValidationError)return res.status(400).json({success:false,error:'INVALID_INVESTMENT_SUBSCRIPTION_COMMAND',message:e.message});return res.status(409).json({success:false,error:'INVESTMENT_SUBSCRIPTION_REJECTED',message:'The subscription intent could not be accepted in its current state.'});}}; }
export const investmentSubscriptionController=new InvestmentSubscriptionController();
