import '@testing-library/jest-dom';import React from'react';import{fireEvent,render,screen,waitFor}from'@testing-library/react';import SavingsWithdrawals from'./SavingsWithdrawals';import{savingsAPI}from'../services/savingsAPI';import{savingsWithdrawalAPI}from'../services/savingsWithdrawalAPI';jest.mock('../services/savingsAPI');jest.mock('../services/savingsWithdrawalAPI');
describe('SavingsWithdrawals',()=>{it('requests minor units and permits cancellation',async()=>{(savingsAPI.listEnrolments as jest.Mock).mockResolvedValue([{enrolment:{id:'e1',currency:'NGN'},product:{name:'Saver'}}]);(savingsWithdrawalAPI.list as jest.Mock).mockResolvedValue([{id:'w1',requested_minor:500000,net_payout_minor:500000,currency:'NGN',state:'pending_approval'}]);(savingsWithdrawalAPI.reviews as jest.Mock).mockResolvedValue([]);(savingsWithdrawalAPI.request as jest.Mock).mockResolvedValue({});(savingsWithdrawalAPI.cancel as jest.Mock).mockResolvedValue({});render(<SavingsWithdrawals/>);await screen.findByText('Saver');fireEvent.change(screen.getByLabelText('Withdrawal amount'),{target:{value:'5000'}});fireEvent.click(screen.getByRole('button',{name:'Request withdrawal'}));await waitFor(()=>expect(savingsWithdrawalAPI.request).toHaveBeenCalledWith('e1',500000));fireEvent.click(await screen.findByRole('button',{name:'Cancel'}));await waitFor(()=>expect(savingsWithdrawalAPI.cancel).toHaveBeenCalled());});});
